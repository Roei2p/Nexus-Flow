# n8n connection (server side only - never exposed to the browser)
N8N_URL=http://n8n:5678
N8N_API_KEY=replace-with-your-n8n-public-api-key

# Dashboard
PORT=3000
# Optional basic auth for the dashboard, format user:password
# DASH_AUTH=admin:change-me
